<%= form_for(@pg) do |f| %>
  <% if @pg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pg.errors.count, "error") %> prohibited this pg from being saved:</h2>

      <ul>
      <% @pg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
